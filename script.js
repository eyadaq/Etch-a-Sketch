let 	numOfSquares= 16;

function 	drawSquares()
{
	let squareSize = 650/numOfSquares;
	let grid = document.querySelector(".grid");

	for (let i = 0; i < numOfSquares; i++)
	{
		for (let x = 0; x < numOfSquares; x++)
		{
			div = document.createElement("div");
		}
	}
}